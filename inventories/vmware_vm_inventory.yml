plugin: vmware_vm_inventory
# vmware inventory plugin inventory, partial example
# This should be operable with an vmware credential in tower as an scm inventory
# PyVmomi, requests>=2.3, vsphere, vcloud must be installed in venv to successfully use plugin
