plugin: azure_rm
# azure_rm inventory plugin inventory, partial example
# This should be operable with an azure credential in tower as an scm inventory
# requires azure>=2.0.0 to be installed in venv to use
