---
- hosts: all
  gather_facts: false
  connection: local
  vars:
    msg: 'hello'
  tasks:
  
    - name: Add host to group 'just_created' with variable foo=42
      ansible.builtin.add_host:
        name: 'hageman'
        groups: just_created

    - debug: "hello {{ inventory_hostname }}"
